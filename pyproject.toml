[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "multimodal-personality-demo"
version = "0.1.0"
description = "Demo multimodal personality & emotion inference"
authors = [ { name = "Demo Author" } ]
requires-python = ">=3.10"
